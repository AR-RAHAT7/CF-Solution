#include <bits/stdc++.h>
using namespace std;

int main(){
	int i,n;
	string S;
	cin>>n;
	int ans=0;
	cin>>S;
	char b=S.at(0);
	for(i=1;i<n;i++)
    {
        if(S.at(i)==b)
        {
            ans++;
        }
        b=S.at(i);
    }
    cout<<ans<<endl;
}
