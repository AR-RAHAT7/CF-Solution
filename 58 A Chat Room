#include<iostream>
using namespace std;
int main()
{
	string s;
	cin>>s;
	string h="hello";
	int k=0;
	for(int i=0;i<s.size();i++){
		if(s[i]==h[k])
			k++;
	}
	if(k==5)
		cout<<"YES";
	else
		cout<<"NO";
}
